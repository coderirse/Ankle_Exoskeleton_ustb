# 全链路测试状态 — 2026-07-29

## 今日变更 (18:30 优化后)

### 1. CAN 层收尾优化 (`can_ankle_node.h`)
- `pthread_create` 后加 `pthread_detach(threadid)`，避免线程资源泄漏
- `can_init()` 开头检查 `usbreset` 是否存在并给出明确警告

### 2. 控制节点优雅退出 (`can_ankleControl_node.cpp`)
- `signalHandler` 中调用 `can_close()`，释放 `VCI_CloseDevice`

### 3. Launch 启动顺序优化 (`start_node.launch.py`)
- 控制节点用 `TimerAction(period=2.0)` 延迟启动，确保力传感器已稳定发布 `/Force`

### 4. 全链路步态实测 ✅
- 启动：`ros2 launch can_ankle start_node.launch.py`
- 预紧完成：`force=1.30`
- 主循环进入 `STAND_MODE` (mode=4)
- 手动发布步态指令后状态机正常切换：
  - `0x41` → `mode=1` (TORQUE_MODE)
  - `0x42` → `mode=2` (PRE_TORQUE_MODE)
  - `0x44` → `mode=0` (VELOCITY_MODE)
- `/Force` 20Hz 稳定发布，控制节点回调实时收到
- `/angle` 正常发布

### 5. 端口持久化映射 ✅
- 新增 `scripts/99-ankle.rules`
- 固定符号链接：
  - `/dev/ankle_encoder` → 编码器
  - `/dev/ankle_switch` → 足底开关
  - `/dev/ankle_force` → 力传感器
  - `/dev/ankle_can` → CANalyst-II
- 已安装到 `/etc/udev/rules.d/99-ankle.rules`，三个串口节点默认改用符号链接

### 6. 已知非致命问题
- 收到 `SIGINT` 时 `serial_ForceSensor_node` / `serial_encoder_node` 偶发 `RCLError: failed to create guard condition`，为 ROS2 多节点同时 shutdown 的已知现象，不影响运行期间功能

## 今日变更 (17:50)

### 1. Launch 文件修复 (`start_node.launch.py`)
- 移除 `prefix='xterm -e'`（xterm 未安装），改为纯后台启动
- 节点启动顺序：力传感器 → 编码器 → 足底开关 → 控制节点
- 控制节点带完整 ROS 参数默认值

### 2. 旧代码清理
- 删除 `src/can_ankle/src/can_ankle_node.cpp`（ROS1 代码，ros::NodeHandle/ros::spinOnce，不在 CMakeLists 中，与新版 header 冲突）

### 3. 力传感器日志降级
- `serial_ForceSensor_node.cpp`: 默认 `debug:=false`
- RX/超时/无响应等日志从 INFO 改为 DEBUG

### 4. CAN 接收线程修复 (关键bug)
- `can_init()` 末尾新增 `pthread_create(&threadid, NULL, receive_func, NULL)`
- 之前 receive_func 只定义未启动，导致 `Output_TorqueValue`/`Output_Angle`/`Output_VelocityValue` 永远为 0
- 影响：SWING 位置控制、位置补偿需此反馈；力保护/预紧不依赖此数据

### 5. 启动脚本
- `scripts/start_all.sh` — 全链路一键启动（传感器→开关→控制节点，含设备检查）
- `scripts/stop_all.sh` — 停止所有节点（支持自动查找 PID）

### 7. 全链路验证 ✅
- 4 节点全部正常运行：`can_ankleControl_node` + `serial_ForceSensor_node` + `serial_encoder_node` + `serial_sendCommand_node`
- 启动方式：`ros2 launch can_ankle start_node.launch.py`
- 控制节点主循环 200Hz 稳定运行（count 递增正常）
- `/torque_info` 正常发布，force_sensortorque=9.06 Nm（力传感器 1.51 × 6.0）
- /Force=1.51（稳定），/angle=1.55°
- **CAN 接收线程**：`return_torque_value`/`return_velocity` 为 0（电机未配置 PDO 主动上报，需后续配置 COB-ID 映射）
- **已修复**：can_recv 超时从 500ms→10ms，VCI_Receive 持锁导致主线程阻塞的问题

### 8. 启动方式变更
- 统一使用 `ros2 launch can_ankle start_node.launch.py`
- 停止：`bash scripts/stop_all.sh --auto`

## 已完成

### 1. CAN 头文件重写 (`can_ankle_node.h`)
- 移除了 TCP bridge（原连接 `127.0.0.1:9876`），改用 `libcontrolcan.so` 原生 VCI API
- 实现 `can_init()` / `can_send()` / `can_recv()` / `can_close()` 基于 `VCI_OpenDevice/InitCAN/StartCAN/Transmit/Receive`
- CANopen 电机使能序列（NMT → pre-op → shutdown → switch on → enable）内联在 `can_init()` 中
- **死锁已修复**：`can_init()` 调用 `can_send()` 时内部锁冲突，将 `std::mutex` 改为 `std::recursive_mutex`

### 2. CMakeLists.txt 更新
- `add_can_exec` 宏新增链接 `${LIBCONTROLCAN_LIB}` (`lib/libcontrolcan.so`) + `dl`

### 3. Launch 文件修复 (`start_node.launch.py`)
- 补上了缺失的 `serial_sendCommand_node`（足底开关 C++ 节点，原已存在，仅 launch 漏配置）

### 4. udev 权限规则
- 创建 `/etc/udev/rules.d/99-canalyst.rules`，将 `04d8:0053` 设备权限设为 `plugdev` 组 `0660`

### 5. USB Reset 集成
- `can_init()` 开头调用 `system("usbreset 04d8:0053")`，因 `libcontrolcan.so` 在新内核上 `libusb_set_configuration()` 会报 "Device or resource busy"，需先 reset

---

## 关键发现

### CAN 设备必须 usbreset
- `libcontrolcan.so` 内调 `libusb_set_configuration()` 失败（内核已自动配置设备），返回 STATUS_ERR(0)
- **唯一有效方案**：启动前执行 `usbreset 04d8:0053`，已通过 `system()` 集成到 `can_init()` 中
- 开发资料位于 `/home/ler/下载/CAN分析仪二次开发示例源代码20250326/`，示例用 `VCI_USBCAN2` (type=4)，与当前一致
- 设备拓扑：`1-2.4`（bus 1, 动态 devnum，当前=008），vid:pid=04d8:0053

### 力传感器正常
- Modbus 0x0FA1 寄存器，响应 `01 03 04 00 00 00 9E 7B 9B` → 原始值 158 → /Force = 1.58
- 当前鲍登线仍存预张力，读取约 1.55-1.63

### 编码器正常
- `/dev/ttyUSB0`，角度 1.55°

### 足底开关链路完整
- `serial_sendCommand_node.cpp` 已是 C++ 实现
- 读取 `/dev/ttyUSB1` 9600bps，解析 0x41-0x45 协议
- 发布 `/command_topic` + `/one_support_time` ~ `/support_time` 等时序话题

---

## 待做

### 高优先级
1. ~~**启动全链路测试** — `ros2 launch can_ankle start_node.launch.py` 通过，步态指令触发状态机正常切换~~ ✅ 已完成
2. ~~**验证 CAN 接收线程** — `pthread_create` 已添加并 `detach`~~ ✅ 已完成
3. ~~**验证电机 SDO 使能** — `can_init()` NMT + CANopen SDO 后电机进入可操作状态，预紧阶段发送速度指令有响应~~ ✅ 已完成
4. **实测电机转动 / 力闭环** — 当前为台架，编码器未跟电机联动，需在机械连接后验证实际助力曲线

### 中优先级
4. ~~安装 xterm 或修改 launch~~ ✅ 已去掉 xterm 前缀
5. **清理其他节点调试输出** — `can_ankleVelocity_node.cpp`, `can_ankleTorqueDriver_node.cpp` 的 narrowing conversion warnings
6. ~~传感器节点日志过多~~ ✅ 已改为默认 debug=false
7. **力传感器解析验证** — scale 0.01, 确认与物理力值吻合

### 低优先级
8. **IMU 集成** — 当前 `can_ankleControl_node` 已订阅 `/imu` 和 `/system_speed` 但未启用（`initializeSlopeFile()` 被注释）
9. **Python 依赖清理** — `can_bridge.py`、`switch_node.py`、`assist_controller.py` 已删除，确认无残留引用
10. **README 与实际对齐** — 更新架构图中的节点名称和端口号

---

## 关键文件

| 文件 | 作用 | 状态 |
|------|------|------|
| `src/can_ankle/include/can_ankle/can_ankle_node.h` | CAN 传输层（libcontrolcan.so 原生） | 已修复死锁 |
| `src/can_ankle/src/can_ankleControl_node.cpp` | 主控制节点（状态机+PID+预紧） | 全链路步态测试通过 |
| `src/can_ankle/CMakeLists.txt` | 构建配置 | 已链接 libcontrolcan.so |
| `src/can_ankle/launch/start_node.launch.py` | 启动文件 | 已补足底开关节点 |
| `/etc/udev/rules.d/99-canalyst.rules` | USB 权限 | 已创建 |
| `docs/WORK_LOG_2026-07-29.md` | 今日工作记录 | 需更新 |
